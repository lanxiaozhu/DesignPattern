package com.todo.subordinatea;

import com.todo.Center;

/**
 * 抽象工厂 和 普通工厂的类似：
 * 都是返回值为 接口  而抽象工厂 是 类工厂实现接口工厂
 * @author Administrator
 *
 */
public interface AbstractFactory {
	public Center printFactory();
}
